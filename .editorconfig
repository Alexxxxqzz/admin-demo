root=true

[*] #表示适合所有文件
end_of_line = lf  #控制换行类型
insert_final_newline = true  #始终在文件末尾插入一个新的行
charset = utf-8   #设置文件字符集
indent_style = space #缩进风格
indent_size = 2 #缩进大小
trim_trailing_whitespace = true #去除行尾的任意空白字符

[*.md] #.md文件适用以下规则
max_line_length = off
trim_trailing_whitespace = false

